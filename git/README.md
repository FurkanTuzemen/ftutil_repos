# git

Status: planned

Installs Git.

- `linux/bootstrap.sh` — installs Git via the OS package manager, host-level, intended to be cloned and run identically on every Raspberry Pi.
- `windows/install.ps1` — installs Git for Windows.

Scaffolded from [`_template`](../_template) — see that folder for the structure convention this project follows.
