* text=auto
*.sh text eol=lf
*.ps1 text eol=crlf

# Extensionless shell scripts: force LF so a Windows checkout can't break the
# shebang when the tree is synced to a Raspberry Pi.
net-failover/linux/net-failover text eol=lf
net-failover/test/bin/* text eol=lf
